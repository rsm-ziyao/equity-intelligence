export interface FinancialSnapshot { period_type: 'quarterly' | 'annual'; fiscal_year: number; fiscal_quarter: number | null; period_start: string | null; period_end: string; filing_date: string | null; revenue: number | null; gross_profit: number | null; operating_income: number | null; net_income: number | null; diluted_eps: number | null; operating_cash_flow: number | null; capital_expenditures: number | null; free_cash_flow: number | null; cash_and_cash_equivalents: number | null; total_debt: number | null; gross_margin: number | null; operating_margin: number | null; profit_margin: number | null; currency: string | null; unit: string | null; provider: string; retrieved_at: string }
export interface FundamentalsResponse { data: { symbol: string; company_name: string | null; financials: { latest_quarterly: FinancialSnapshot | null; latest_annual: FinancialSnapshot | null }; provenance: { provider: string; retrieved_at: string | null; freshness: string } } | null; meta: { available: boolean; missing_metrics: string[]; periods_returned: number } }
export interface FinancialTrendPeriod extends FinancialSnapshot { revenue_yoy_growth: number | null; net_income_yoy_growth: number | null; eps_yoy_growth: number | null; free_cash_flow_yoy_growth: number | null }
export interface MissingPeriod { label: string; period_end: string; missing_metrics: string[] }
export interface FinancialHistoryData { symbol: string; company_name: string | null; period_type: 'quarterly' | 'annual'; periods: FinancialTrendPeriod[]; provenance: { provider: string; retrieved_at: string | null; freshness: string } }
export interface FinancialHistoryMeta { available: boolean; periods_returned: number; requested_limit: number; missing_metrics: string[]; metric_coverage: Record<string, number>; missing_periods: MissingPeriod[] }
export interface FinancialHistoryResponse { data: FinancialHistoryData | null; meta: FinancialHistoryMeta }
